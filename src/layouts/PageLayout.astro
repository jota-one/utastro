---
import SiteLayout from "./SiteLayout.astro";
import type { LangCode } from '@/routes'

interface Props {
  lang?: LangCode
}
const { lang } = Astro.props
---

<SiteLayout {lang}>
  <slot />
</SiteLayout>
