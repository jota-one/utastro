# Astro SSR app URL (test/staging instance)
BASE_URL=http://localhost:4321

# PocketBase URL (test/staging instance)
PB_URL=http://localhost:8091

# ID of an event that exists in the test DB (not full, not cancelled)
TEST_EVENT_ID=

# ID of a city that exists in the test DB
TEST_CITY_ID=
